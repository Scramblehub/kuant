# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Testing
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.hypothesis/
coverage.xml

# CUDA build artifacts
*.cubin
*.fatbin
*.ptx
*.cudafe*
*.cu.o

# Virtual environments
.env
.venv
env/
venv/
ENV/

# Data (never commit — kuant is code-only)
*.parquet
*.hdf5
*.h5
*.csv
# Root-anchored so kuant/data/ (the subpackage) and tests/data/
# (its tests) aren't masked. Unanchored `data/` used to hide both.
/data/
/raw/

# Secrets — API tokens, credentials (NEVER commit)
secret
secrets/
.secrets/
.env.local
*.token
*.credentials
.pypirc

# Benchmark runtime artifacts (results streamed locally, not committed)
benchmarks/results/latest.jsonl
benchmarks/results/manifest.json
benchmarks/queue.log
benchmarks/.queue.lock
.benchmarks/

# Docs build
docs/_build/
docs/_static/
docs/_templates/
site/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local notes / scratch
scratch/
notes.local.md
